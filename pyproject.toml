[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "siestaflow_hubbard"
version = "0.1.0"
dependencies = [
    "numpy>=1.20.0,<2.0.0"
]

[project.optional-dependencies]
test = [
    "pytest>=7.0.0"
]
